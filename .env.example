# =========================================================================
# CLINICAL SYSTEMS CONTROL PLANE - LOCAL CONFIGURATION EXAMPLE
# =========================================================================

# 1. CORE INTELLIGENCE SERVICES (Server-Side)
# Required for generating smart clinical abstract OCR summaries in lockbox.
# AI Studio automatically injects this from your secret locker.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# 2. APPLICATION CONFIGURATION
# base URL for the web workspace environment.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# =========================================================================
# FUTURE PRODUCTION INFRASTRUCTURE: AWS cloud services
# (These will be populated when shifting from mock local DB to live AWS)
# =========================================================================

# 3. AWS COMPATIBILITY & REGIONAL ROUTER
AWS_REGION="us-east-1"

# 4. AWS COGNITO DIRECTORY IDENTITY (User Authentication)
AWS_COGNITO_USER_POOL_ID="us-east-1_abcdef123"
AWS_COGNITO_CLIENT_ID="1234567890abcdefghijklmnop"

# 5. AWS API GATEWAY & LAMBDA WORKLOAD RUNTIMES
AWS_API_GATEWAY_URL="https://api-gateway.us-east-1.amazonaws.com/prod"

# 6. AMAZON AURORA CLINICAL DB REGISTRY (PostgreSQL interface)
# Replace mock local 'db.json' with actual durable SQL engines
DATABASE_URL="postgresql://db_user:db_password@aurora-cluster.cluster-xyz.us-east-1.rds.amazonaws.com:5432/clinic_db"

# 7. AMAZON S3 HEALTHCARE RECORD LOCKBOX (HIPAA Compliant Document Store)
AWS_S3_BUCKET_NAME="clinical-records-lockbox-production"

# 8. AWS WORKLOAD SERVICE ACCOUNT AUTHORIZATIONS (IAM Credentials)
# When hosted on Vercel, populate these. (On AWS Lambda / ECS, utilize IAM Instance Profiles instead).
AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"
AWS_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
